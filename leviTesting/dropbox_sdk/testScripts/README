File descriptions in this directory:
1. dropbox_token.txt 
   it stores the token for the user after running the program: request_dropbox_token.py
2. request_dropbox_token.py 
   program to run that captures and saves the dropbox token so user does not have to
   authorize application every session
3. sessionUpload.py
   test application that uploads working-draft.txt file to dropbox folder: Apps/fileUploader_linkCreator/ and
   creates a link to the txt file in the folder and prints it out to you
4. working-draft.txt
   a txt file that is uploaded to Dropbox by sessionUpload.py program. Nothing important really.
